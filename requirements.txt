# ──────────────────────────────────────────────────────────────
#  Multi-Source RAG — Python Dependencies
#  Install all:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# Core framework
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# ── LLM Providers (install only what you use) ──────────────────
langchain-groq>=0.2.0           # Free tier: https://console.groq.com
langchain-google-genai>=2.0.0   # Free tier: https://aistudio.google.com
langchain-openai>=0.2.0         # Paid
langchain-ollama>=0.2.0         # Local / free

# ── Embeddings ─────────────────────────────────────────────────
langchain-huggingface>=0.1.0    # Free local embeddings (default)
sentence-transformers>=3.0.0    # Required for HuggingFace embeddings

# ── Vector Stores ──────────────────────────────────────────────
langchain-chroma>=0.1.0         # Free local (default)
chromadb>=0.5.0
langchain-qdrant>=0.1.0         # Free local or cloud
qdrant-client>=1.9.0
langchain-pinecone>=0.2.0       # Cloud (free tier available)
pinecone-client>=4.0.0

# ── Web Search ─────────────────────────────────────────────────
duckduckgo-search>=6.0.0        # Free, no API key (default)
tavily-python>=0.3.0            # Free tier: 1000 searches/month
google-search-results>=2.4.0    # SerpAPI (paid)

# ── Source Loaders ─────────────────────────────────────────────
youtube-transcript-api>=0.6.0   # YouTube transcripts (free)
pymupdf>=1.24.0                 # PDF parsing (free)
docx2txt>=0.8                   # DOCX parsing (free)
beautifulsoup4>=4.12.0          # Web scraping
gitpython>=3.1.0                # GitHub repo cloning

# ── API & Frontend ─────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
streamlit>=1.38.0
python-multipart>=0.0.9         # File uploads in FastAPI
aiofiles>=23.0.0                # Required for FastAPI FileResponse

# ── Utilities ──────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.32.0
