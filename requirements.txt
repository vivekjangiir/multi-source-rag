# ──────────────────────────────────────────────────────────────
#  Multi-Source RAG — Core Dependencies (default free stack)
#  Python 3.11 required
# ──────────────────────────────────────────────────────────────

# Core LangChain
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# LLM — Groq (free default)
langchain-groq>=0.2.0

# LLM — NVIDIA NIM (free, 100+ models, no credit card)
langchain-nvidia-ai-endpoints>=0.3.0

# LLM — optional extras (uncomment to enable)
# langchain-google-genai>=2.0.0
# langchain-openai>=0.2.0
# langchain-ollama>=0.2.0
# langchain-anthropic>=0.3.0

# Embeddings — HuggingFace local (free default)
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# Vector Store — ChromaDB local (free default)
langchain-chroma>=0.1.0
chromadb>=0.5.0

# Vector Store — optional extras (uncomment to enable)
# langchain-qdrant>=0.1.0
# qdrant-client>=1.9.0
# langchain-pinecone>=0.2.0
# pinecone-client>=4.0.0

# Web Search — DuckDuckGo (free, no key needed)
duckduckgo-search>=6.0.0

# Web Search — optional extras (uncomment to enable)
# tavily-python>=0.3.0

# Source Loaders
youtube-transcript-api>=0.6.0
yt-dlp>=2024.1.0
pymupdf>=1.24.0
docx2txt>=0.8
beautifulsoup4>=4.12.0
requests>=2.32.0

# GitHub loader
gitpython>=3.1.0

# API & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=23.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
